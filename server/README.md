# Server (API)

Express + MongoDB API for Learning Tracker. **Render uses this folder** as the service root.

## Commands

```bash
npm install
cp .env.example .env
npm run dev
```

## Layout

- `server.js` — entry point
- `src/` — controllers, models, routes, middleware, utils
- `scripts/` — one-off maintenance scripts
