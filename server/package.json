{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "description": "Render compatibility entry — use backend/ as the service root when possible.",
  "scripts": {
    "postinstall": "npm install --prefix ../backend",
    "start": "node server.js"
  }
}
