# Client (frontend)

React + Vite app. Deployed on **Vercel** with root directory `client`.

```bash
npm run dev      # from repo root, or npm run dev here
npm run build
```

- Local: http://localhost:3000
- API proxy: `/api` → `http://localhost:5001` (dev only)

Copy `.env.example` → `.env.local` only if you need overrides (e.g. Google OAuth).
